name: Proxy Multi-Sieve Inspector (Triggered)

on:
  repository_dispatch:
    types: [proxy_updated]
  workflow_dispatch:

jobs:
  sieve_process:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Sieve Orchestrator
        run: python main.py
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Commit and Push Results
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "Elite-Inspector-Bot"
          git add .
          # Коммитим только при наличии изменений (предотвращает ошибку пустого коммита)
          git diff --quiet && git diff --staged --quiet || (git commit -m "Deep Audit Complete (Native Sieve Mode): $(date)" && git push)
